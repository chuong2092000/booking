server:
  port: ${CONFIG_SERVER_PORT:8888}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: ${GIT_URI:https://gitlab.com/chuonggiang2209/config-server-files.git}
          default-label: ${GIT_LABEL:main}
          username: ${GIT_USERNAME:chuonggiang2209}
          password: ${GIT_PASSWORD:glpat-ZCUoohNmTndBvOG477XhzGM6MQpvOjEKdTpuZXZzeg8.01.1712q25cg}

eureka:
  client:
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_HOST:http://localhost}:${EUREKA_PORT:8761}/eureka/
    register-with-eureka: true

management:
  endpoints:
    web:
      exposure:
        include: "refresh,health,info"